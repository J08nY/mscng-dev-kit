Sample program for strong key using CNG.

It creates a new RSA key-pair, and sets the strong key policy
on the new key pair. After the key-pair generation is completed,
the sample uses the key-pair. The use of the key causes a UI
prompt to be displayed, asking for a password.
